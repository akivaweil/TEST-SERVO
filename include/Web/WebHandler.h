#ifndef WEBHANLDER_H
#define WEBHANLDER_H

// Include necessary headers (add more as needed)
#include <Arduino.h>
#include <ESPAsyncWebServer.h>
#include <WebSocketsServer.h>

// Function declarations (add function prototypes used by main.cpp)
// void webSocketEvent(uint8_t num, WStype_t type, uint8_t * payload, size_t length); // Example if needed

#endif // WEBHANLDER_H 